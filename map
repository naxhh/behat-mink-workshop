$ ./bin/behat --init

- Create behat.yml (empty?)
- Use the default FeatureContext to perform api tests.

- Create WebContext
- Now what??? How we told what should use?
- Specify Suites in behat.yml!
- Configure suites
$ ./bin/behat --suite=api_features

- Create web tests using goutte.
- Perform headless? tests.

- Now test messages.
- Ups! does not work!
- Explain js tests
- Test using selenium2

